export interface MenuContainerProps {
  minimized: boolean;
  showMenu: boolean;
}

export interface MenuItemProps {
  active: boolean;
}

export interface MenuItemAccordionContainerProps {
  show: boolean;
  height: number;
}

export interface MenuItemAccordionProps {
  active: boolean;
}
